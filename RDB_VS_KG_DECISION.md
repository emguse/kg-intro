# RDB vs Knowledge Graph Decision Guide

このドキュメントは、製造業の機械装置、BOM、製作番号、サプライヤー、ロット、FSE/サポート履歴を扱うときに必ず出てくる「RDB で良いのではないか」という議論を整理するためのものです。

結論は、RDB と KG を対立させないことです。RDB は正本データや業務トランザクションに強く、KG は部門やシステムをまたいだ横断探索、トレーサビリティ、意味づけに強いです。

## Short Answer

多くのデータは RDB で管理できます。特に、部品マスタ、購買発注、入荷ロット、S-BOM 明細、検査履歴のような業務データは RDB と相性が良いです。

ただし、次のような問いが増えるなら、KG を読み取り用の横断モデルとして持つ価値があります。

- 設計BOM、製造BOM、実績BOMの差分をたどりたい
- サプライヤー起点で影響する製作番号を探したい
- 不具合ロットから対象装置、部品、FSE事例まで逆展開したい
- エラーコード、症状、原因、対処、FAQを装置構成とつなげたい
- 部門ごとに違う台帳を、共通の意味で横断検索したい

## Positioning

おすすめの分担:

```text
RDB
  正本データ
  業務更新
  トランザクション
  マスタ管理
  権限、承認、監査

KG
  読み取り用の横断モデル
  トレーサビリティ
  影響範囲分析
  意味づけ
  部門横断の探索
```

KG は RDB を置き換えるものではありません。既存の RDB、CSV、Excel、PLM、ERP、CMMS、サービス記録をつなぐ読み取り層として考えると、導入の摩擦が小さくなります。

## Where RDB Is Strong

RDB が向いているもの:

- 部品マスタ
- サプライヤーマスタ
- 購買発注
- 入荷、検収、在庫
- 製作番号台帳
- S-BOM 明細
- 検査結果
- 承認ワークフロー
- 定型帳票
- 更新整合性が重要な業務処理

RDB が得意な問い:

```text
この製作番号のS-BOM明細を一覧したい
この部品の標準サプライヤーを見たい
この発注番号の明細を見たい
このロットの入荷日と検査結果を見たい
今月の購買実績を集計したい
```

これらは RDB の方が素直です。無理に KG に寄せる必要はありません。

## Where KG Is Strong

KG が効きやすいのは、関係の種類が増え、問いが横断的になる領域です。

KG が得意な問い:

```text
不具合ロットに関係しそうな製作番号、装置、部品、FSE事例をたどりたい
設計部品と実績部品が違う箇所から、代替理由、購買先、過去不具合まで見たい
あるエラーコードに関係する部品、工程、過去対処、対象モデルを横断したい
サプライヤー変更が、どの装置、工程、保全手順、FAQに影響するか見たい
廃番部品が、どの製作番号、どの顧客、どのサポート履歴に関係するか知りたい
```

KG の価値は「RDB で不可能だから」ではありません。固定された帳票や JOIN ではなく、後から増える関係をたどりやすいことにあります。

## A Useful Phrase

議論の場では、次の言い方が使いやすいです。

```text
RDB は台帳。
KG は台帳同士をつなぐ地図。
```

もう少し業務寄りに言うなら:

```text
RDB は分かっている業務を正確に処理する。
KG は後から出てくる横断的な問いに答える。
```

## Decision Questions

RDB で十分か、KG を検討すべきかは、次の質問で切り分けます。

### RDBで十分なサイン

- テーブル構造が安定している
- 問いが定型である
- JOIN の範囲が決まっている
- 更新、承認、集計が中心である
- SQL で十分に保守できる
- 参照するシステムが少ない
- 業務トランザクションが主目的である

### KGを検討するサイン

- 問いが増え続ける
- 関係の種類が増え続ける
- 部門ごとに語彙や粒度が違う
- 複数の既存DBやファイルを横断探索したい
- 3 hop, 4 hop の影響範囲分析が増える
- エラーコード、症状、作業メモ、FAQなど半構造データも混ぜたい
- 「この情報とこの情報が実はつながっている」を後から足したい

## BOM Traceability Example

E-BOM、M-BOM、S-BOM、サプライヤー、ロットを扱う場合、次のように分けます。

### RDBの正本に向いているもの

| Data | Reason |
| --- | --- |
| 部品マスタ | 品番、名称、仕様、改訂の管理が必要 |
| サプライヤーマスタ | 取引先コード、名称、購買条件を安定管理する |
| 購買発注 | 業務トランザクションとして正確性が重要 |
| 入荷ロット | 検収、在庫、品質記録と結び付く |
| S-BOM明細 | 製作番号ごとの実績として正確性が重要 |

### KGに向いている読み取りモデル

| Traversal | Use |
| --- | --- |
| E-BOM -> M-BOM -> S-BOM | 設計、製造、実績の差分確認 |
| Supplier -> BOMLine -> SerializedBOM -> ManufacturingSerial | サプライヤー起点の影響範囲 |
| Lot -> BOMLine -> ManufacturingSerial | 不具合ロットの対象製作番号 |
| Part -> ServiceCase -> Countermeasure | 部品起点の過去対応検索 |
| ErrorCode -> Symptom -> Part -> WorkProcedure | FSE/サポート支援 |

## Architecture Pattern

低コストに始めるなら、次の構成が現実的です。

```text
ERP / PLM / 購買DB / 品質DB / サービスDB
        |
        | extract
        v
JSON-LD snapshot files
        |
        | validate / build index
        v
SQLite / DuckDB / reverse index
        |
        | optional
        v
RDFLib / SPARQL / GraphDB PoC
```

最初から GraphDB を本番投入しなくても、JSON-LD と読み取り用インデックスでかなり学べます。

重要なのは、正本をどこに置くかです。

```text
正本: RDB、PLM、ERP、購買DB、品質DB
KG: 横断検索用の読み取りモデル
```

## File-Based KG and RDB

製作番号ごとにスナップショットした S-BOM があり、部品や明細をキーで確実に捕捉できるなら、最初はファイルベース KG でも始められます。

```text
snapshots/
  sn-2026-0001/
    sbom.jsonld
    metadata.json
  sn-2026-0002/
    sbom.jsonld
    metadata.json

index/
  by_part.json
  by_supplier.json
  by_lot.json
```

ただし、逆展開が日常業務になるなら、JSON-LD ファイルだけでは弱くなります。

その場合は、JSON-LD を正本または交換形式として残しつつ、SQLite、DuckDB、RDB、GraphDB などに読み取り用インデックスを作ります。

## Common Objections

### RDBでJOINすればいいのでは

定型 JOIN なら RDB で十分です。

ただし、今回見たいのが固定帳票ではなく、部品、ロット、設計差分、問い合わせ、FSE対応などを後から増える関係で横断探索することなら、読み取り用 KG が効きます。

### KGを入れると正本が増えて混乱しないか

混乱します。だから KG を正本にしない設計から始めます。

RDB や既存業務システムを正本にし、KG は抽出、変換、検証された読み取りモデルとして扱います。

### GraphDBまで必要なのか

最初から必要とは限りません。

まずは JSON-LD、validation、reverse index、SQLite/DuckDB で十分です。GraphDB は、日常的な多段探索、SPARQL、推論、権限付きWeb利用などが必要になってから評価します。

### SQLとKGの両方を覚えるのは重いのでは

重いです。

そのため、最初は KG を「新しいDB」ではなく、「関係を明文化した読み取りモデル」として学ぶのがよいです。JSON-LD や表形式の edge list から始めれば、RDB 経験者にも入りやすくなります。

## Recommended Policy

KG導入検討プロジェクトでは、次の方針が現実的です。

- RDB を否定しない
- GraphDB を最初から前提にしない
- JSON-LD/KG を読み取り用の横断モデルとして作る
- 正本は既存システムや RDB に置ける設計にする
- 逆展開が必要なら reverse index や DuckDB/SQLite を作る
- KG の価値は、問いと関係が増える領域で検証する

まとめると:

```text
RDBで管理できるものはRDBでよい。
KGは、RDB同士、ファイル、履歴、サービス記録を横断してたどるために使う。
```

この線引きがあると、「RDBで良いじゃないか」議論は敵ではなく、KG の使いどころを絞るための良いフィルターになります。
